<?xml version="1.0"?>

    <menu tools:context="com.huantnguyen.radcases.app.CaseListActivity" xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Search, should appear as action button -->
    <item android:title="@string/action_search" android:actionViewClass="android.widget.SearchView" android:showAsAction="ifRoom" android:icon="@drawable/ic_action_search" android:id="@+id/action_search"/>

    <!-- Add new case, should appear as action button -->
    <item android:title="@string/action_new" android:showAsAction="ifRoom" android:icon="@drawable/ic_action_new" android:id="@+id/action_new"/>

    <!-- Settings, should always be in the overflow -->
    <item android:title="@string/action_settings" android:showAsAction="never" android:id="@+id/action_settings"/>

</menu>