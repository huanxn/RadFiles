<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/default_background_light_grey"
    tools:context="com.radicalpeas.radfiles.app.ManageListsActivity.ViewPagerFragment"
    android:layout_marginLeft="16dp">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="vertical"
        android:background="?attr/colorPrimary"
        android:layout_alignParentTop="true"
        />
    <!-- uses list_item_sortable.xml textview-->

    <!--
    <LinearLayout android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:layout_margin="0dp"
                  android:paddingLeft="16dp"
                  android:paddingRight="16dp"
                  android:background="@color/default_background_light_grey"
                  android:orientation="horizontal"
                  android:layout_alignParentBottom="true"
                  >

    <ImageView
        android:id="@+id/handle"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:src="@drawable/ic_plus_circle_grey600_18dp"/>

    <TextView
        android:id="@+id/item_text"
        android:text="list item text"
        style="@style/listSortable"
        android:layout_weight="1"/>

    </LinearLayout>
    -->

</RelativeLayout>
